DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=Cookmate
ML_PORT=8001
# Comma-separated list of allowed Express origins (local + production)
# Example production: EXPRESS_ORIGIN=http://localhost:5000,https://your-api.onrender.com
EXPRESS_ORIGIN=http://localhost:5000
# Secret key required in X-ML-API-Key header for all ML endpoints (leave empty to disable in dev)
ML_API_KEY=
